version: '3'
services:
  express-app:
    container_name: express-app
    build: .
    ports:
      - "3000"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - npm_network

networks:
  npm_network:
    external: true
